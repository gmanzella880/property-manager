# vcs / tooling
.git
.gitignore
.gitattributes
.sf
.vercel

# secrets - never bake these into the image, they are injected at runtime
.env
.env.*

# build output & deps (rebuilt inside the image)
.next
node_modules
npm-debug.log

# not needed at build time
Dockerfile
docker-compose.yml
README.md
AGENTS.md
CLAUDE.md
